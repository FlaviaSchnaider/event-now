import axios from 'axios';

// export const api = axios.create({
//   baseURL: 'http://localhost:5000/api'
// });

export const api = axios.create({
  baseURL: 'https://eventnow.onrender.com/api'  // substitua se necessário
});
